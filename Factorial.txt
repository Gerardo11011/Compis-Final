begin

func int factorial(int n, int acum) {
  output(n);
  if (n<=1) {
    return acum;
  }
  acum = n * (factorial(n-1, acum););
}

main {
  int n, res;
  output("Factorial:");
  input(n);
  res = factorial(n, 0);
  output(res);
}

end
