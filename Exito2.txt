begin

func int recursividad(int p, int q){
  if (p < 10){
    if (q < 10){
      if (q == p){
        output(p);
      }
    }
    recursividad(p+1, q);
  }
  return p;
}

main {
  int p, q;
  p = 0;
  q = 0;

  recursividad(p, q);
}

main {
   int num;
   output("Enter the number : ");
   input(num);
   output("The fibonacci series : ");
   fib(num);
}
end
